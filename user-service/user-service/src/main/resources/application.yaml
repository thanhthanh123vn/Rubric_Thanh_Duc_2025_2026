
server:
  port: 8083

spring:
  application:
    name: rubric-service
  datasource:

    url: jdbc:mysql://localhost:3306/rubric_db?useSSL=false
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # Tr? v? Eureka Server