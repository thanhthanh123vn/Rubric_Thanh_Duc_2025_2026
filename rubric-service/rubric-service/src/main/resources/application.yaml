server:
  port: 8081

spring:
  application:
    name: RUBRIC-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/elearning
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/